# Firebase Configuration (REQUIRED)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Google Calendar Integration (OPTIONAL)
# 1. Create OAuth 2.0 credentials (Web application) in Google Cloud Console
# 2. Enable Google Calendar API
# 3. In OAuth consent screen → Add scopes: calendar, calendar.events
# 4. If you get "insufficient permissions", disconnect and reconnect in Settings
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com

# Development: Use Firebase Emulators (OPTIONAL)
# Set to 'true' to use local emulators instead of production Firebase
# This gives you UNLIMITED reads/writes during development
# Run: firebase emulators:start
# NEXT_PUBLIC_USE_EMULATOR=true
